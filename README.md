# weather-etl-pipeline
End-to-end data engineering pipeline: extracts weather data from Open-Meteo API, transforms and loads into SQLite, runs analytics, and aut-updates daily via GitHub Actions. 100% free.
